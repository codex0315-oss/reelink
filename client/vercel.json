{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "comment": "Every path serves index.html so React Router can resolve it in the browser. Without this, opening /dashboard directly — or refreshing while on it — returns a 404, because Vercel looks for a dashboard/index.html that a single-page build never produces. Real files are matched first, so hashed assets under /assets are unaffected.",
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "comment": "Vite puts a content hash in every asset filename, so a given URL can never change. Caching them for a year costs nothing and a deploy invalidates them by producing new names.",
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "comment": "index.html must never be cached: it is the file that points at the hashed assets, so a stale copy would keep serving the previous deploy's JavaScript.",
      "source": "/index.html",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }
      ]
    }
  ]
}
